[project]
name = "CountdownClock"
version = "0.1.0"
description = "A minimalist, always-on-top countdown timer with silent visual alerts for Windows and macOS"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pyinstaller>=6.16.0",
]
